include:
  # Донгл (на базе nice!nano v2)
  - board: nice_nano_v2
    shield: jorne_dongle
  
  # Левая половинка
  - board: nice_nano_v2
    shield: jorne_left
    
  # Правая половинка
  - board: nice_nano_v2
    shield: jorne_right
